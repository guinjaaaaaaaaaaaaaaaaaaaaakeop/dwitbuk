---
description: Findings from runs, gates, delegations, non-claims and the tree since the last review
argument-hint: [--since COMMIT]
---
!`python3 "${CLAUDE_PLUGIN_ROOT}/dwitbuk.py" review $ARGUMENTS --target .`

Show the output above to the user as is. Do not interpret or act on it unless asked.
